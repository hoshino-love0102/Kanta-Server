spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: service-meeting
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/kanta_meeting}
    username: ${DB_USERNAME:kanta}
    password: ${DB_PASSWORD:kanta}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP_ID:service-meeting}
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true

server:
  port: ${SERVER_PORT:8085}

kanta:
  kafka:
    topics:
      meeting-summarized: ${KAFKA_TOPIC_MEETING_SUMMARIZED:meeting.summarized}
  outbox:
    publisher-enabled: ${OUTBOX_PUBLISHER_ENABLED:true}
    poll-size: ${OUTBOX_POLL_SIZE:50}
    poll-delay-ms: ${OUTBOX_POLL_DELAY_MS:5000}
  kanban:
    base-url: ${KANBAN_SERVICE_URL:http://service-kanban:8081}
  ai:
    base-url: ${AI_SERVICE_URL:http://localhost:8000}
    timezone: ${AI_TIMEZONE:Asia/Seoul}

spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: service-workspace
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/kanta_workspace}
    username: ${DB_USERNAME:kanta}
    password: ${DB_PASSWORD:kanta}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP_ID:service-workspace}
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true

server:
  port: ${SERVER_PORT:8084}

kanta:
  kafka:
    topics:
      member-updated: ${KAFKA_TOPIC_MEMBER_UPDATED:member.updated}
  outbox:
    publisher-enabled: ${OUTBOX_PUBLISHER_ENABLED:true}
    poll-size: ${OUTBOX_POLL_SIZE:50}
    poll-delay-ms: ${OUTBOX_POLL_DELAY_MS:5000}
